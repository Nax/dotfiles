if [ -f "$HOME/.config/shell/rc.sh" ]; then
    source "$HOME/.config/shell/rc.sh"
fi

source "$HOME/.config/zsh/options.zsh"
source "$HOME/.config/zsh/completion.zsh"
source "$HOME/.config/zsh/prompt.zsh"
